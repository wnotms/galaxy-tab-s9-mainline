# SM-X710 第十一次完整首次镜像对照

本次使用第一次实际写入的完整四镜像，直接从首次归档复制。boot、init_boot、vendor_boot、dtbo 均逐字节一致，不重新封装；初始四补丁内核、初始 DTB、无 initcall_debug 命令行均保持一致。

第一次取得主线日志后发生固件 NoC 错误；第二至第十次没有取得主线输出。此前矩阵尚缺原内核／原命令行组合的重复试验。本次先复验这个组合，不能把它称为完整启动成功的镜像。

TWRP 预检通过，四镜像写入后逐个完整回读匹配，recovery / vbmeta 未变，已正常重启。Windows 未观察到 Linux / Samsung USB、新网卡或 ADB。用户返回 TWRP 后日志已回收，原四分区已恢复并完整回读匹配，最终六个分区哈希与原分区一致，设备留在 TWRP。完整镜像和日志位于 `artifacts/boot-tests/eleventh-sm-x710/`。

完整四镜像逐字节一致，但没有重复取得主线输出。last_kmsg 为 2097136 字节，SHA256 `1193fdb24aa687d7b40325fe31a8d9bbdf1d98e1a5a738d9661bce80e99c4c73`；没有主线版本、NoC 致命标记或三处保留区错误，命令行没有 initcall_debug。首次内核不包含后续早期检查点，标记缺失不是本次判据。没有另行取得屏幕状态确认。

这证明仅回放首次软件组合没有恢复可观察输出，仍不能区分实际内核入口、启动环境及复位后的日志保留行为。不能继续将问题归为后来 DTS 或诊断补丁的唯一回归。下一次测试对齐 Ultra 的 EFI／可重定位启动支持；断电冷启动仍是后续需隔离的启动状态变量。
